var year = document.getElementById("year_selection");
alert( year.options[ year.selectedIndex ].value)

var term = document.getElementById("term_selection");
alert( term.options[ term.selectedIndex ].value )

var core_recs = document.getElementById("core_recs_selection");
alert( core_recs.options[ core_recs.selectedIndex ].value )

var dept = document.getElementById( "dept_selection" );
alert( dept.options[ dept.selectedIndex ].value )

var professor = document.getElementById( "professor_selection" );
alert( professor.options[ professor.selectedIndex ].value )

